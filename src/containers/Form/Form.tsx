import { useParams } from "react-router-dom";

const Form = ()=>{
    const param = useParams();
    console.log(param.id);
    
    
    return(
        <>
        
        </>
    )
}

export default Form;